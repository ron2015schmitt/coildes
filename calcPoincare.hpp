
#include "matricks.hpp"

bool calcPoincare(Matricks::Vector<Matricks::p3vector<double> >& position,
		  const Matricks::p3vector<double>& startPosition,
		  int& numTransits,
		  const int pointsPerTransit);

bool calc_1pt_Poincare(Matricks::p3vector<double> & finalposition,
		       const Matricks::p3vector<double>& startPositionRZP);



