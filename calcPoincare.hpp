
#include "cooll.hpp"

bool calcPoincare(COOLL::LAvector<COOLL::p3vector<double> >& position,
		  const COOLL::p3vector<double>& startPosition,
		  int& numTransits,
		  const int pointsPerTransit);

bool calc_1pt_Poincare(COOLL::p3vector<double> & finalposition,
		       const COOLL::p3vector<double>& startPositionRZP);



