void inductancematrix(const LAvector<p3vector<double> >& X1, const LAvector<p3vector<double> >& n1,
		      const LAvector<p3vector<double> >& X2, const LAvector<p3vector<double> >& n2, 
		      Matrix<double>& M);


